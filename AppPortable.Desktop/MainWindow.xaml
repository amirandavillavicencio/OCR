<Window x:Class="AppPortable.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:AppPortable.Desktop.ViewModels"
        mc:Ignorable="d"
        Title="AppPortable" Height="760" Width="1320">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="320" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="8" Margin="4">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top">
                    <TextBlock Text="Documentos" FontSize="18" Margin="4" />
                    <Button Content="Cargar PDF" Command="{Binding LoadDocumentCommand}" />
                    <Button Content="Reindexar" Command="{Binding ReindexCommand}" />
                    <TextBlock Text="Estado OCR" FontSize="14" Margin="4,10,4,0" />
                    <TextBlock Text="{Binding OcrStatus}" TextWrapping="Wrap" Margin="4" />
                </StackPanel>
                <ListBox ItemsSource="{Binding Documents}" DisplayMemberPath="SourceFile" Margin="4" />
            </DockPanel>
        </Border>

        <Border Grid.Column="1" Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="8" Margin="4">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
                    <TextBox Width="460" Text="{Binding Query, UpdateSourceTrigger=PropertyChanged}" FontSize="14" />
                    <Button Content="Buscar" Command="{Binding SearchCommand}" />
                </StackPanel>
                <ProgressBar DockPanel.Dock="Bottom" Height="12" Minimum="0" Maximum="100" Value="{Binding Progress}" Margin="4" />
                <TextBlock DockPanel.Dock="Bottom" Text="{Binding Status}" Margin="4" />

                <ListBox ItemsSource="{Binding Results}" SelectedItem="{Binding SelectedDetail}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="{StaticResource AccentBrush}" BorderThickness="1" CornerRadius="6" Margin="4" Padding="8">
                                <StackPanel>
                                    <TextBlock Text="{Binding SourceFile}" FontWeight="Bold" />
                                    <TextBlock Text="{Binding Snippet}" TextWrapping="Wrap" />
                                    <TextBlock Text="{Binding Score, StringFormat='Score: {0:F4}'}" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Border>

        <Border Grid.Column="2" Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="8" Margin="4">
            <StackPanel>
                <TextBlock Text="Detalle" FontSize="18" Margin="4" />
                <ContentControl Content="{Binding SelectedDetail}">
                    <ContentControl.Resources>
                        <DataTemplate DataType="{x:Type vm:MainViewModel}" />
                        <DataTemplate DataType="{x:Type sys:Object}" xmlns:sys="clr-namespace:System;assembly=mscorlib">
                            <TextBlock Text="{Binding}" TextWrapping="Wrap" />
                        </DataTemplate>
                    </ContentControl.Resources>
                </ContentControl>
            </StackPanel>
        </Border>
    </Grid>
</Window>
