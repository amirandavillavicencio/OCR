<Window x:Class="AppPortable.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:AppPortable.Desktop.ViewModels"
        mc:Ignorable="d"
        Title="AppPortable" Height="720" Width="1280" MinHeight="620" MinWidth="1024">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="240" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="320" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Margin="8" Padding="8" Style="{StaticResource PanelBorderStyle}">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Bottom" Margin="0,12,0,0">
                        <Button Content="Cargar PDF" Command="{Binding LoadDocumentCommand}" Height="36" Margin="0,0,0,8" />
                        <Button Content="Reindexar" Command="{Binding ReindexCommand}" Height="36" />
                    </StackPanel>

                    <TextBlock Text="Documentos" FontWeight="SemiBold" Margin="0,0,0,8" />
                    <ListBox ItemsSource="{Binding Documents}"
                             SelectedItem="{Binding SelectedDocument}"
                             DisplayMemberPath="Display"
                             Margin="0,24,0,0" />
                </DockPanel>
            </Border>

            <Border Grid.Column="1" Margin="8" Padding="8" Style="{StaticResource PanelBorderStyle}">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="480" Height="34" Margin="0,0,8,0" />
                        <Button Content="Buscar" Command="{Binding SearchCommand}" Width="96" Height="34" />
                    </StackPanel>

                    <ListView ItemsSource="{Binding Results}" SelectedItem="{Binding SelectedSearchResult}">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Archivo" Width="200" DisplayMemberBinding="{Binding SourceFile}" />
                                <GridViewColumn Header="Pág." Width="70" DisplayMemberBinding="{Binding PageStart}" />
                                <GridViewColumn Header="Snippet" Width="520" DisplayMemberBinding="{Binding Snippet}" />
                            </GridView>
                        </ListView.View>
                    </ListView>
                </DockPanel>
            </Border>

            <Border Grid.Column="2" Margin="8" Padding="8" Style="{StaticResource PanelBorderStyle}">
                <StackPanel>
                    <TextBlock Text="Detalle" FontWeight="SemiBold" Margin="0,0,0,8" />
                    <TextBox Text="{Binding SelectedResultDetails}" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Height="560" />
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="#FFEF5350" TextWrapping="Wrap" Margin="0,8,0,0" />
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="1" Margin="8,0,8,8" Padding="8" Style="{StaticResource PanelBorderStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="220" />
                    <ColumnDefinition Width="120" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding StatusMessage}" VerticalAlignment="Center" />
                <ProgressBar Grid.Column="1" Value="{Binding ProgressValue}" Height="18" Margin="8,0" Minimum="0" Maximum="100" />
                <TextBlock Grid.Column="2" Text="{Binding ProgressValue, StringFormat={} {0}%}" VerticalAlignment="Center" HorizontalAlignment="Right" />
            </Grid>
        </Border>
    </Grid>
</Window>
